@AGENTS.md

## Agent skills

### Issue tracker

Issues for this repo live as GitHub Issues on `Christ-m18/tienda_ropa_online`. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the default five-label vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo — one `CONTEXT.md` at the root (create with `/grill-with-docs` when ready) and `docs/adr/` for architectural decisions. See `docs/agents/domain.md`.
